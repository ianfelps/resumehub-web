# Base URL of the ResumeHub .NET API (include the /api suffix).
# Local dev:  http://localhost:5087/api
# Production: set this in the Vercel dashboard to the deployed API, e.g.
#   https://resumehub-api.onrender.com/api
NEXT_PUBLIC_API_BASE_URL=http://localhost:5087/api
